/** Project constants. App-owned — never overwritten by template updates. */
export const PROJECT_NAME = "dhcp-app";
export const PROJECT_TITLE = "DHCP Monitor";
export const PROJECT_DESCRIPTION = "Real-time DHCP lease monitoring for homelab environments";
export const DEFAULT_BACKEND_PORT = 5000;
export const DEFAULT_SSE_GATEWAY_PORT = 3001;
export const DEFAULT_FRONTEND_PORT = 3000;

/** Set to false to hide the sidebar and hamburger menu. */
export const SIDEBAR_VISIBLE = false;
