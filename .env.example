# API Configuration
# For development with separate backend server
VITE_API_BASE_URL=http://localhost:5000/api/v1
# For production (uses relative URLs via NGINX proxy)
# VITE_API_BASE_URL=/api/v1

# Development settings
VITE_DEV_MODE=true